@model Account
@* @if (!ViewData.ModelState.IsValid)
{
    <script>
        $(document).ready(function () {
            var modal = new bootstrap.Modal(document.getElementById('editPasswordModal'));
            modal.show();
        });
    </script>
}
@if (!ViewData.ModelState.IsValid)
{
    <script>
        $(document).ready(function () {
            var modal = new bootstrap.Modal(document.getElementById('editEmailModal'));
            modal.show();
        });
    </script>
}

@if (!ViewData.ModelState.IsValid)
{
    <script>
        $(document).ready(function () {
            var modal = new bootstrap.Modal(document.getElementById('editNameModal'));
            modal.show();
        });
    </script>
}
 *@

@* @if (ViewData.ModelState.ContainsKey("HashPassword") && ViewData.ModelState["HashPassword"].Errors.Count > 0)
{
    <script>
        $(document).ready(function () {
            var modal = new bootstrap.Modal(document.getElementById('editPasswordModal'));
            modal.show();
        });
    </script>
} *@

@if (ViewData.ModelState.ContainsKey("Email") && ViewData.ModelState["Email"].Errors.Count > 0)
{
    <script>
        $(document).ready(function () {
            var modal = new bootstrap.Modal(document.getElementById('editEmailModal'));
            modal.show();
        });
    </script>
}

@if (ViewData.ModelState.ContainsKey("UserName") && ViewData.ModelState["UserName"].Errors.Count > 0)
{
    <script>
        $(document).ready(function () {
            var modal = new bootstrap.Modal(document.getElementById('editNameModal'));
            modal.show();
        });
    </script>
}

<div class="main-content">
    <!-- Top Navbar -->
    <div class="top-navbar">
        <div class="d-flex justify-content-between align-items-center">
            <h2 class="h4 mb-0">Account Information</h2>
            <div class="dropdown">
                <button class="btn btn-outline-secondary rounded-circle" type="button" data-bs-toggle="dropdown">
                    <i class="bi bi-person"></i>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li>
                        <a class="dropdown-item" asp-controller="Account" asp-action="Index">
                            <i class="bi bi-person me-2"></i>Profile
                        </a>
                    </li>
                    <li>
                        <hr class="dropdown-divider">
                    </li>
                    <li>
                        <a class="dropdown-item text-danger" asp-controller="Account" asp-action="Login">
                            <i class="bi bi-box-arrow-right me-2"></i>Logout
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

    <!-- Main Content Area -->
    <div class="container-fluid py-4">
        <div class="row">
            <div class="col-md-8">
                <div class="card info-card mb-4">
                    <div class="card-body">
                        <h3 class="h5 mb-4">Account Information</h3>

                        <div class="mb-4">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <span class="info-label">Email Address</span>
                                <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal"
                                        data-bs-target="#editEmailModal">
                                    <i class="bi bi-pencil"></i> Edit
                                </button>
                            </div>
                            <div class="info-value">@Model.Email</div> <!-- Display logged-in user's email -->
                        </div>

                        <div class="mb-4">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <span class="info-label">Full Name</span>
                                <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal"
                                        data-bs-target="#editNameModal">
                                    <i class="bi bi-pencil"></i> Edit
                                </button>
                            </div>
                            <div class="info-value">@Model.UserName</div> <!-- Display full name -->
                        </div>

                       @*  <div class="mb-4">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <span class="info-label">Password</span>
                                <button class="btn btn-outline-primary btn-sm" data-bs-toggle="modal"
                                        data-bs-target="#editPasswordModal">
                                    <i class="bi bi-pencil"></i> Edit
                                </button>
                            </div>
                            <div class="info-value">••••••••••••••••</div> <!-- Display full name -->
                        </div> *@

                        <div class="mb-4">
                            <div class="d-flex justify-content-between align-items-center mb-2">
                                <span class="info-label">Account Created</span>
                            </div>
                            <div class="info-value">@Model.Created_at.ToString("MMMM dd, yyyy")</div> <!-- Display account creation date -->
                        </div>
                    </div>
                    @await Html.PartialAsync("_EditEmailModal", Model)
                    @await Html.PartialAsync("_EditNameModal", Model)
                    @* @await Html.PartialAsync("_EditPasswordModal", Model) *@
                </div>

                <div class="d-flex justify-content-end">
                    <a asp-controller="Account" asp-action="Login" class="btn btn-danger">
                        <i class="bi bi-box-arrow-right me-2"></i>Logout
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}